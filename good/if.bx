// if-else

proc main() {
  var x = 1, y = 2, z = 3 : int;

  if (x > y) { print(1); }
  else { print(-1); }

  if (x + y == z) { print(999); }

  if      (x == 0) { print(0);  }
  else if (x == 1) { print(1);  }
  else if (x == 2) { print(2);  }
  else             { print(42); }
}