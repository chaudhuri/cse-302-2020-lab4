// relational operators

proc main() {
  var x = 42 : int;
  var y = -42 : int;

  if (x == x) { print(0); }
  if (x != y) { print(1); }
  if (x < y)  { print(2); }
  if (x <= y) { print(3); }
  if (x > y)  { print(4); }
  if (x >= y) { print(5); }
}